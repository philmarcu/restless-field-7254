<h1><%= @airline.name %> Current Passengers</h1>
<% @airline.adult_passengers.each do |pass| %>
  <div id="pass-<%= pass.id %>">
    <% if pass.age >= 18 %>
      <p><%= pass.name %> - Age <%= pass.age %></p>
    <% end %>
  </div>
<% end %>